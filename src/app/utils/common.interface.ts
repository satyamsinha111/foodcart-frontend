export interface Order {
  orderid: string;
  item: string;
  price: number;
  date: Date;
}

export interface item {
  itemid: string;
  item_name: string;
  item_desc: string;
  item_price: string;
}
